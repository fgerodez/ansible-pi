{{ item.hostname }} {  
  reverse_proxy {{ item.target }}

  log {
    output stderr
  }

  {% if item.secure is defined and item.secure %}
  basicauth /* {
    {{ item.user }} {{ item.password }}
  }
  {% endif %}
}